package wg_meta::Commands;
use strict;
use warnings FATAL => 'all';

use experimental 'signatures';
use base 'Exporter';
our @EXPORT = qw(command_show);


sub command_show($ref_attr_list, $ref_wg_meta_attr_list, $ref_parsed_config, $ref_parsed_show){

}

1;